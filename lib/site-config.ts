// ============================================================
// ЦЕНТРАЛЬНЫЙ ФАЙЛ НАСТРОЕК АЙА
// ============================================================
// Здесь собраны все ссылки, контакты и подтверждённые данные
// о бренде. Когда появятся реальные ссылки/номера — впишите их
// сюда, и они автоматически появятся во всех местах сайта
// (шапка, подвал, кнопки, страница контактов).
//
// Поля со значением null означают "данных пока нет" — в этом
// случае соответствующая кнопка/блок на сайте не показывается
// или показывается как временно скрытая, а не ведёт в никуда.
// ============================================================

export const siteConfig = {
  brand: {
    name: "АЙА", // название на русском
    nameEn: "AIA", // название на английском (для латиницы, метаданных, доменных нужд)
    country: "Кыргызстан",
    city: "Бишкек",
    // Адрес производства (НЕ юридический адрес)
    productionAddress: "с. Лебединовка, ул. Мичурина, 127",
  },

  // Ссылки на внешние площадки. Пока не заполнены — кнопки скрыты.
  links: {
    wildberries: "https://www.wildberries.ru/seller/3995290" as string | null,
    telegramChannel: "https://t.me/aia_factory_kg" as string | null,
  },

  // Контакты для связи с покупателями и партнёрами.
  contacts: {
    // Несколько контактных телефонов с именами (могут отвечать разные люди).
    phones: [
      { name: "Тамара", number: "+99679040176" },
      { name: "Нестана", number: "+996 554 723 233" },
    ] as { name: string; number: string }[],
    // Номера WhatsApp разделены по регионам: свой номер для СНГ и свой для Европы.
    whatsapp: {
      cis: "996709040176" as string | null, // WhatsApp для стран СНГ
      europe: "996705883366" as string | null, // WhatsApp для европейских стран
    },
    email: "dzheenbekova18@mail.ru" as string | null,
  },

  // Условия контрактного производства, которые можно открыто публиковать.
  contractProduction: {
    minBatch: 300,
    direction: "Производство женских изделий широкого спектра",
  },

  // ------------------------------------------------------------
  // Юридические данные. Пока не публикуются на сайте.
  // Когда данные появятся — заполните и подключите к странице
  // "Политика конфиденциальности" / подвалу сайта.
  // ------------------------------------------------------------
  legal: {
    entityName: null as string | null, // [ЗАПОЛНИТЬ ПОЗЖЕ] — юридическое наименование
    inn: null as string | null, // [ЗАПОЛНИТЬ ПОЗЖЕ] — ИНН
    registrationNumber: null as string | null, // [ЗАПОЛНИТЬ ПОЗЖЕ] — регистрационный номер
    legalAddress: null as string | null, // [ЗАПОЛНИТЬ ПОЗЖЕ] — юридический адрес
    bankDetails: null as string | null, // [ЗАПОЛНИТЬ ПОЗЖЕ] — банковские реквизиты
    personalDataOperator: null as string | null, // [ЗАПОЛНИТЬ ПОЗЖЕ] — данные оператора персональных данных
  },
};

export type SiteConfig = typeof siteConfig;

// Ссылка WhatsApp для конкретного региона (или null, если номера нет).
export function getWhatsAppLink(region: "cis" | "europe"): string | null {
  const number = siteConfig.contacts.whatsapp[region];
  if (!number) return null;
  return `https://wa.me/${number.replace(/\D/g, "")}`;
}
